import { type Widget } from '@/types';
interface MetricCardProps {
    widget: Widget;
}
export declare const MetricCard: ({ widget }: MetricCardProps) => import("react/jsx-runtime").JSX.Element;
export {};
